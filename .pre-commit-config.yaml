# Secret scanning via gitleaks.
# Blocks commits that introduce secrets (API keys, tokens, app secrets).
# Run manually across all files: pre-commit run --all-files
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
